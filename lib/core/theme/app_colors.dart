import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF0A0A0A);
  static const surface = Color(0xFF161616);
  static const card = Color(0xFF1F1F1F);

  // Accent — lime green (energetic, health-forward)
  static const accent = Color(0xFFB8FF65);
  static const accentMuted = Color(0x26B8FF65); // 15% opacity

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF888888);
  static const textTertiary = Color(0xFF444444);

  // Semantic
  static const danger = Color(0xFFFF5C5C);
  static const success = Color(0xFF65FFB0);

  // Structural
  static const border = Color(0xFF252525);
  static const divider = Color(0xFF1E1E1E);
  static const overlay = Color(0xCC000000);
}
